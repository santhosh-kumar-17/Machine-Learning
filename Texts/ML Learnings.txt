ML Learnings:

df.descibe()
describing what and all is in the dataframe and it is giving min max sd mean etc

df['Date'] = pd.to_datetime(df['Date'])
to make date from being object to Date datatype

moving average(Indicator)
